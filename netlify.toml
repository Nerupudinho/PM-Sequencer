[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Netlify's Next.js runtime handles the .next directory automatically
[[plugins]]
  package = "@netlify/plugin-nextjs"
